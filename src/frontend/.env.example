# Data Source Configuration
# Set to 'mock' for built-in sample data or 'external' for real API data
VITE_DATA_SOURCE_MODE=mock

# External API Configuration (required only when VITE_DATA_SOURCE_MODE=external)
# Replace with your actual API endpoint and key
VITE_EXTERNAL_API_URL=https://api.example.com/restaurants
VITE_EXTERNAL_API_KEY=your_api_key_here
